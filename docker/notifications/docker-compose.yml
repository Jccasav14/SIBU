services:
  notifications:
    env_file:
    - ../env/common.env
    - ../env/notifications.env
    ports:
    - 8004:8004
    networks:
    - sibu-net
    image: ${DOCKERHUB_USER:-jccasav}/sibu-notifications:${SIBU_TAG:-qa}
    pull_policy: if_not_present
networks:
  sibu-net:
    name: sibu-net
    external: true
