services:
  reports:
    env_file:
    - ../env/common.env
    - ../env/reports.env
    ports:
    - 8007:8007
    networks:
    - sibu-net
    image: ${DOCKERHUB_USER:-jccasav}/sibu-reports:${SIBU_TAG:-qa}
    pull_policy: if_not_present
networks:
  sibu-net:
    name: sibu-net
    external: true
