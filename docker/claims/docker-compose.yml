services:
  claims:
    env_file:
    - ../env/common.env
    - ../env/claims.env
    ports:
    - 8009:8009
    networks:
    - sibu-net
    image: ${DOCKERHUB_USER:-jccasav}/sibu-claims:${SIBU_TAG:-qa}
    pull_policy: if_not_present
networks:
  sibu-net:
    name: sibu-net
    external: true
