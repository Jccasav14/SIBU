services:
  admin:
    env_file:
    - ../env/common.env
    - ../env/admin.env
    ports:
    - 8008:8008
    networks:
    - sibu-net
    image: ${DOCKERHUB_USER:-jccasav}/sibu-admin:${SIBU_TAG:-qa}
    pull_policy: if_not_present
networks:
  sibu-net:
    name: sibu-net
    external: true
