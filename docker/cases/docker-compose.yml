services:
  cases:
    env_file:
    - ../env/common.env
    - ../env/cases.env
    ports:
    - 8003:8003
    networks:
    - sibu-net
    image: ${DOCKERHUB_USER:-jccasav}/sibu-cases:${SIBU_TAG:-qa}
    pull_policy: if_not_present
networks:
  sibu-net:
    name: sibu-net
    external: true
